import {Dimensions} from 'react-native';

const dimensions = {
  widthWindow: Dimensions.get('screen').width,
  heighWindow: Dimensions.get('screen').height,
  heightHeaderDisplayTempScreen: 65,
};

export default dimensions;
